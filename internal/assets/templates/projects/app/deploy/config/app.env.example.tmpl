# Runtime configuration for {{.App}}
# ──────────────────────────────────────────────────────────────────────────────
# This is the example / template config. Copy it to app.env and edit for your
# environment. Run `make config-init` or `kt config init` to do this automatically.
#
# These KEY=VALUE pairs are loaded by the systemd EnvironmentFile directive and
# are available to the process as environment variables.
#
# For structured config (YAML, INI, .properties), add additional *.example files
# alongside this one — postinstall.sh and `make config-init` will copy them all.
# ──────────────────────────────────────────────────────────────────────────────

# Runtime environment
APP_ENV=production

# Log level: debug, info, warn, error
# LOG_LEVEL=info
