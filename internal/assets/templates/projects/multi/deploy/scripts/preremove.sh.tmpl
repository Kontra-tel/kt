#!/usr/bin/env bash
# preremove.sh — runs before the package is removed from the target machine.
set -euo pipefail

systemctl stop {{.App}}-backend {{.App}}-frontend || true
systemctl disable {{.App}}-backend {{.App}}-frontend || true
systemctl daemon-reload
