name: ${APP}
arch: ${ARCH}
platform: linux
version: ${VERSION}
section: default
priority: optional
maintainer: {{.Author}}
description: ${APP} CLI
contents:
  - src: dist/app/usr/local/bin/${APP}
    dst: /usr/local/bin/${APP}
    file_info:
      mode: 0755
overrides:
  rpm:
    group: Applications/System
